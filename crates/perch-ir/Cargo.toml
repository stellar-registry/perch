[package]
name = "perch-ir"
description = "Perch policy document model: canonical JSON, doc_hash, validation"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
hex = "0.4"
