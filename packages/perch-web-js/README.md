# `@stellar-registry/perch-web`

This package parses `perch-web/v1` documents. It produces stable policy hashes
and matched BrowserPlan and ServerPlan values. It also supplies fail-closed call
checks for the two enforcement points.

See [`../../WEB_POLICY.md`](../../WEB_POLICY.md) for the profile and integration
contract.
